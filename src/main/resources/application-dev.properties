# Activated only with --spring.profiles.active=dev (or SPRING_PROFILES_ACTIVE=dev).
# Turns on the Google-verification bypass at POST /api/v1/auth/dev-google-test.
# Do not set this profile active in any staging/production environment.
app.dev-auth.enabled=true
