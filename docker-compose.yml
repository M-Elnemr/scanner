services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_NAME:-umrah_scanner}
      POSTGRES_USER: ${DB_USERNAME:-umrah_scanner}
      POSTGRES_PASSWORD: ${DB_PASSWORD:?set DB_PASSWORD in .env}
    volumes:
      - db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME:-umrah_scanner} -d ${DB_NAME:-umrah_scanner}"]
      interval: 5s
      timeout: 5s
      retries: 10

  app:
    build: .
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: ${DB_NAME:-umrah_scanner}
      DB_USERNAME: ${DB_USERNAME:-umrah_scanner}
      DB_PASSWORD: ${DB_PASSWORD:?set DB_PASSWORD in .env}
      JWT_SECRET: ${JWT_SECRET:?set JWT_SECRET in .env}
      GOOGLE_CLIENT_IDS: ${GOOGLE_CLIENT_IDS:-}
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE:-}
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS:-*}
      UPLOADS_DIR: /app/uploads
    volumes:
      - app_uploads:/app/uploads
    ports:
      - "127.0.0.1:${APP_PORT:-8080}:8080"

  web:
    build:
      context: ./web
      args:
        NEXT_PUBLIC_GOOGLE_CLIENT_ID: ${WEB_GOOGLE_CLIENT_ID:-}
    restart: unless-stopped
    depends_on:
      - app
    environment:
      API_BASE_URL: http://app:8080
      SESSION_SECRET: ${WEB_SESSION_SECRET:?set WEB_SESSION_SECRET in .env}
    ports:
      - "127.0.0.1:${WEB_PORT:-3000}:3000"

volumes:
  db_data:
  app_uploads:
